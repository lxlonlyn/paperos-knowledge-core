# Data Model

## Purpose

This document defines the shared PaperOS data model.

The same model definitions are used by:

* PDF ingestion;
* MinerU result transformation;
* canonical persistence;
* Cognee DataPoints;
* lexical and vector indexes;
* graph retrieval;
* comprehensive query;
* feedback and knowledge improvement.

The data model is independent of deployment and test history.

## Model layers

PaperOS distinguishes four logical data layers:

1. source artifacts;
2. parser artifacts;
3. canonical document objects;
4. semantic and derived knowledge.

These layers belong to one continuous provenance chain.

```
SourceFile
→ ParseRun
→ CanonicalSnapshot
→ Document objects
→ Cognee DataPoints
→ Derived knowledge
→ Query evidence
```

## Common persistence fields

Every persisted object must include the fields that apply to its role.

### Identity fields

* `id`
* `schema_version`
* `id_version`

### Lifecycle fields

* `created_at`
* `updated_at`
* `status`

### Provenance fields

* `source_file_id`
* `parse_run_id`
* `canonical_snapshot_id`
* `document_id`
* `section_id`
* `chunk_id`
* `element_id`
* `derived_from_ids`
* `created_by`
* `model`
* `model_version`
* `pipeline_version`

Optional provenance fields must remain absent or null when they do not apply. Placeholder IDs must not be generated.

## Source artifacts

## SourceFile

Represents one immutable input file.

Required fields:

* `id`
* `sha256`
* `original_filename`
* `stored_filename`
* `media_type`
* `size_bytes`
* `storage_path`
* `created_at`
* `schema_version`
* `id_version`

Optional fields:

* `source_url`
* `user_metadata`
* `dataset_id`

Identity rule:

* the primary identity is derived from file SHA-256 and the declared ID version;
* ingesting the same bytes must resolve to the same SourceFile identity.

The stored source file must not be modified after registration.

## IngestionJob

Represents one requested ingestion execution.

Required fields:

* `id`
* `source_file_id`
* `dataset_id`
* `status`
* `current_operation`
* `created_at`
* `updated_at`
* `attempt_count`

Optional fields:

* `error_code`
* `error_message`
* `completed_at`
* `requested_options`

Supported statuses:

* `pending`
* `validating`
* `parsing`
* `normalizing`
* `writing`
* `indexing`
* `postprocessing`
* `completed`
* `failed`
* `cancelled`

An IngestionJob is operational state. It is not a knowledge object.

## Parser artifacts

## ParseRun

Represents one MinerU parsing execution for one SourceFile.

Required fields:

* `id`
* `source_file_id`
* `provider`
* `backend`
* `status`
* `request_options`
* `created_at`
* `completed_at`
* `artifact_manifest_path`
* `schema_version`
* `pipeline_version`

Optional fields:

* `provider_task_id`
* `provider_version`
* `provider_model`
* `error_code`
* `error_message`
* `raw_metadata`

A SourceFile may have multiple ParseRuns.

Each new OCR configuration, provider version, or explicit reprocessing request may create a new ParseRun.

## ParserArtifact

Represents one immutable artifact returned or generated by the parser operation.

Supported artifact types include:

* `markdown`
* `content_list`
* `model_output`
* `asset`
* `task_metadata`
* `provider_response`
* `archive`
* `other`

Required fields:

* `id`
* `parse_run_id`
* `artifact_type`
* `storage_path`
* `sha256`
* `size_bytes`
* `created_at`

Optional fields:

* `media_type`
* `page`
* `provider_name`
* `provider_metadata`

Unknown MinerU fields may be preserved in raw parser metadata.

Downstream modules must not depend directly on unknown provider fields.

## Canonical document model

## CanonicalSnapshot

Represents one complete versioned canonical transformation of a ParseRun.

Required fields:

* `id`
* `source_file_id`
* `parse_run_id`
* `document_id`
* `schema_version`
* `pipeline_version`
* `cleaning_version`
* `classification_version`
* `reference_processing_version`
* `manifest_path`
* `created_at`

A ParseRun may produce multiple CanonicalSnapshots when processing rules change.

Canonical snapshots may be destructively rebuilt during pre-stable development. The associated SourceFile and ParserArtifacts must remain preserved.

## Persistent path semantics

`SourceFile.storage_path`, `ParseRun.artifact_manifest_path`,
`ParserArtifact.storage_path`, and `CanonicalSnapshot.manifest_path` are runtime
`Path` values in domain models. Their SQLite representation is a data-root-
relative POSIX reference encoded and decoded only by `DataPathCodec`. Canonical
JSON `asset_path` and index-manifest `lexical_database` use the same reference
system. Absolute paths, Windows drive prefixes, backslashes, and `..` are never
valid persistent values. The public API omits these implementation fields.

## Document

Represents the normalized document as a whole.

Required fields:

* `id`
* `source_file_id`
* `parse_run_id`
* `canonical_snapshot_id`
* `document_type`
* `language`
* `title`
* `created_at`
* `schema_version`

Optional fields:

* `subtitle`
* `abstract`
* `publication_date`
* `year`
* `venue`
* `publisher`
* `doi`
* `arxiv_id`
* `isbn`
* `issn`
* `volume`
* `issue`
* `pages`
* `keywords`
* `authors`
* `affiliations`
* `provider_metadata`

`Paper` may be represented as a specialized Document type or as a Document with `document_type = "paper"`.

The implementation must use one consistent representation throughout ingestion, Cognee, and query.

## Person

Represents an identified person.

Required fields:

* `id`
* `display_name`
* `schema_version`

Optional fields:

* `given_name`
* `family_name`
* `name_parts`
* `orcid`
* `aliases`
* `raw_name`

Names must not assume that every person has exactly one given name and one family name.

## Organization

Represents an institution, affiliation, laboratory, company, publisher, or other organization.

Required fields:

* `id`
* `name`
* `schema_version`

Optional fields:

* `aliases`
* `country`
* `department`
* `raw_name`

## Authorship

Represents the relation between a Document and a Person.

Required fields:

* `id`
* `document_id`
* `person_id`
* `author_order`
* `schema_version`

Optional fields:

* `corresponding`
* `affiliation_ids`
* `raw_text`
* `source_element_id`

Authorship should not duplicate the Person object.

## Section

Represents one normalized section or subsection.

Required fields:

* `id`
* `document_id`
* `canonical_snapshot_id`
* `title`
* `level`
* `order`
* `path`
* `schema_version`

Optional fields:

* `parent_section_id`
* `page_start`
* `page_end`
* `source_span`
* `section_type`
* `raw_title`

The section path must be deterministic within the same canonical schema and processing version.

## Element

Represents one typed document element.

Supported element types include:

* `title`
* `paragraph`
* `formula`
* `figure`
* `table`
* `caption`
* `footnote`
* `code`
* `list`
* `list_item`
* `reference`
* `header`
* `footer`
* `page_number`
* `other`

Required fields:

* `id`
* `document_id`
* `canonical_snapshot_id`
* `element_type`
* `order`
* `schema_version`

Optional fields:

* `section_id`
* `parent_element_id`
* `text`
* `raw_text`
* `markdown`
* `latex`
* `html`
* `asset_path`
* `page`
* `bounding_box`
* `source_span`
* `caption_element_ids`
* `footnote_element_ids`
* `metadata`

Element-specific fields should be represented through typed optional fields or a validated extensible metadata structure.

The implementation must not assume that every element contains plain text.

## Chunk

Represents one retrieval unit derived from canonical sections and elements.

Required fields:

* `id`
* `document_id`
* `canonical_snapshot_id`
* `text`
* `order`
* `schema_version`
* `chunking_version`
* `element_ids`

Optional fields:

* `section_id`
* `section_path`
* `page_start`
* `page_end`
* `token_count`
* exact element-local spans with `character_start_in_element`,
  `character_end_in_element`, `token_start`, and `token_end`
* `previous_chunk_id`
* `next_chunk_id`
* `overlap_source_chunk_ids`
* `metadata`

A Chunk must retain references to its canonical elements.

Chunk text is a retrieval representation, not a replacement for the underlying elements.

Changing chunking rules may create new Chunk IDs under a new chunking or ID version.

## ReferenceEntry

Represents one bibliography or citation-list entry extracted from a document.

Required fields:

* `id`
* `document_id`
* `canonical_snapshot_id`
* `raw_text`
* `order`
* `schema_version`

Optional fields:

* `title`
* `authors`
* `year`
* `venue`
* `publisher`
* `doi`
* `arxiv_id`
* `url`
* `volume`
* `issue`
* `pages`
* `source_element_id`
* `resolved_document_id`
* `resolution_status`
* `resolution_confidence`
* `parsed_fields`

Supported resolution statuses:

* `unresolved`
* `candidate`
* `resolved`
* `ambiguous`
* `rejected`

The original reference text must always be preserved. The legacy
`resolved_document_id` field remains readable within canonical schema 1.0, but
it is not citation identity truth and is not projected to Cognee. Authoritative
resolution is the `reference_work_links` record and
`ReferenceDataPoint.resolved_work_id`.

A failed or ambiguous Work resolution must not create a false CITES relation.

## Semantic knowledge model

## Entity

Represents a semantic object extracted from canonical evidence.

Examples include:

* method;
* model;
* algorithm;
* dataset;
* metric;
* mathematical concept;
* software;
* material;
* task;
* application domain.

Required fields:

* `id`
* `entity_type`
* `name`
* `status`
* `schema_version`
* `derived_from_ids`

Optional fields:

* `description`
* `aliases`
* `normalized_name`
* `confidence`
* `source_chunk_ids`
* `source_element_ids`
* `model`
* `model_version`

Supported statuses:

* `extracted`
* `inferred`
* `user_confirmed`
* `rejected`
* `superseded`

An Entity must not be treated as source fact unless supported by source evidence.

## Claim

Represents a statement extracted or inferred from one or more source objects.

Required fields:

* `id`
* `text`
* `status`
* `schema_version`
* `derived_from_ids`
* `source_chunk_ids`

Optional fields:

* `claim_type`
* `subject_entity_ids`
* `object_entity_ids`
* `confidence`
* `scope`
* `model`
* `model_version`
* `supersedes_claim_id`

A Claim must distinguish:

* directly extracted statements;
* system inference;
* user-confirmed knowledge.

## ConceptRelation

Represents a typed semantic relation.

Required fields:

* `id`
* `relation_type`
* `source_object_id`
* `target_object_id`
* `status`
* `schema_version`
* `derived_from_ids`

Optional fields:

* `description`
* `confidence`
* `source_chunk_ids`
* `source_element_ids`
* `model`
* `model_version`

Examples of relation types include:

* `MENTIONS`
* `USES`
* `EXTENDS`
* `COMPARES_WITH`
* `EVALUATES_ON`
* `PROPOSES`
* `SUPPORTS`
* `CONTRADICTS`
* `DERIVED_FROM`
* `RELATED_TO`

Relation types must be centrally declared.

Modules must not create private relation-name variants for the same meaning.

## Summary

Represents a derived summary.

Required fields:

* `id`
* `summary_type`
* `text`
* `status`
* `schema_version`
* `derived_from_ids`

Optional fields:

* `document_id`
* `section_id`
* `chunk_ids`
* `entity_ids`
* `model`
* `model_version`
* `prompt_version`

Summary types may include:

* document summary;
* section summary;
* topic summary;
* global summary;
* comparative summary.

Summaries are derived views and must not replace source content.

## ResearchInsight

Represents a versioned system-generated or user-confirmed research insight.

Required fields:

* `id`
* `text`
* `status`
* `schema_version`
* `derived_from_ids`

Optional fields:

* `supporting_claim_ids`
* `supporting_chunk_ids`
* `contradicting_claim_ids`
* `confidence`
* `model`
* `model_version`
* `supersedes_insight_id`

## Feedback

Represents user evaluation of an answer, claim, relation, or insight.

Required fields:

* `id`
* `feedback_type`
* `target_id`
* `created_at`
* `schema_version`

Optional fields:

* `query_id`
* `answer_id`
* `evidence_ids`
* `comment`
* `replacement_text`
* `created_by`

Supported feedback types:

* `accept`
* `reject`
* `correct`
* `confirm`
* `mark_unsupported`

## Correction

Represents a versioned correction derived from feedback.

Required fields:

* `id`
* `target_id`
* `replacement_or_correction`
* `status`
* `created_at`
* `schema_version`
* `derived_from_feedback_id`

Optional fields:

* `source_chunk_ids`
* `supersedes_object_id`

Corrections must not overwrite original source objects.

## ScholarlyWork identity

`Document` is one parsed PaperOS document instance; `ScholarlyWork` is the
stable identity of the academic result it represents. A Work may exist without
a PDF and may be represented by zero or more Documents. Its random
`work_<uuid>` ID and schema/id version are persisted once in `registry.db`.
DOI, arXiv, normalized title/year/first-author, document links, reference links,
and merge redirects support deterministic reconciliation. Redirected IDs remain
readable and canonicalize to the survivor; merge priority is ingested,
identified, then provisional, with older creation time breaking ties.

Work identity is retained state, not a derived index. Cognee rebuild consumes
Canonical plus the Work registry and must reproduce the same Work IDs and
citation edges. Only the Work title is vector-indexed in the first projection
version. A `ScholarlyWorkDataPoint` has no canonical snapshot provenance
because an external cited Work may have no local source artifact.

## Required structural relations

The unified graph must support the following core relations:

* `SourceFile PRODUCED ParseRun`
* `ParseRun PRODUCED CanonicalSnapshot`
* `CanonicalSnapshot DESCRIBES Document`
* `Document CONTAINS Section`
* `Section CONTAINS Element`
* `Section CONTAINS Chunk`
* `Chunk REPRESENTS Element`
* `Document AUTHORED_BY Person`
* `Person AFFILIATED_WITH Organization`
* `ReferenceEntry BELONGS_TO Document`
* `Document REPRESENTS_WORK ScholarlyWork`
* `ReferenceEntry RESOLVES_TO ScholarlyWork`
* `ScholarlyWork CITES ScholarlyWork`
* `Chunk MENTIONS Entity`
* `Document PROPOSES Entity`
* `Claim SUPPORTED_BY Chunk`
* `Claim CONTRADICTS Claim`
* `ConceptRelation DERIVED_FROM Chunk`
* `Summary DERIVED_FROM source object`
* `ResearchInsight DERIVED_FROM Claim or Chunk`
* `Correction DERIVED_FROM Feedback`

Every relation derived through an LLM must preserve evidence and model provenance.

## DataPoint mapping

Cognee DataPoint declarations must be centralized in:

```
paperos_core/adapters/cognee/datapoints.py
```

Cognee-specific DataPoint models remain in the Cognee adapter. Canonical-backed
nodes share snapshot/source/parse/chunk provenance, while
`ScholarlyWorkDataPoint` inherits only the provider-neutral graph identity and
derivation fields.

The mapping must explicitly declare:

* identity fields;
* embeddable fields;
* relation fields;
* optional fields;
* deduplication rules;
* provenance fields.

Objects that do not have reliable identity information must not be aggressively deduplicated.

## Cognee dataset binding

Every persisted DataPoint graph is bound to Cognee's relational provenance
models:

* User: Cognee's default principal in the supported single-user deployment;
* Dataset: the canonical snapshot's `dataset_id` name;
* Data: one item identified from source SHA-256 and Cognee user identity;
* DatasetData: membership of that source item in the selected Dataset;
* PipelineRun: one versioned structured-write execution;
* PipelineContext: the context passed to every node and custom edge write.

The Cognee manifest records these IDs and the graph/relational provenance
counts. It is an audit projection, not the source used to infer Dataset
membership. A rebuild recreates all bindings from the retained canonical snapshot,
immutable SourceFile, and persistent Work registry. It never deletes or
reallocates ScholarlyWork identities.

## Index representations

Lexical and vector indexes are projections of canonical or semantic objects.

Each index record must include:

* indexed object ID;
* object type;
* schema version;
* index version;
* source document ID;
* indexed field name;
* model or tokenizer version when applicable.

Indexes must not become independent authoritative records.

## Schema evolution

The project is in pre-stable development.

The following may change:

* canonical object fields;
* element classification;
* chunking;
* ReferenceEntry parsing;
* DataPoint definitions;
* relation types;
* embedding dimensions;
* stable-ID rules.

Every incompatible change must update the relevant version.

The system may destructively rebuild:

* canonical snapshots;
* Cognee stores;
* graph data;
* vector indexes;
* lexical indexes;
* summaries;
* exported views.

The system must preserve:

* original PDFs;
* source checksums;
* raw MinerU outputs;
* ParseRun manifests.

## Extensibility requirements

* OCR fields that may be absent must remain optional.
* The model must support papers without DOI, affiliations, figures, formulas, or resolved references.
* Person names must support structures beyond first-name/last-name.
* Raw reference text must remain available after parsing.
* Raw MinerU metadata may preserve unknown fields.
* Canonical modules must not expose provider-specific raw structures.
* Query code must use canonical IDs rather than filesystem paths as identity.
* Ingestion and query must import the same object definitions.
* New element types and relation types must be introduced centrally.
* Compatibility adapters must not duplicate authoritative objects.
