[pytest]
addopts = --strict-markers
markers =
    external: requires retained real data, a real Cognee pipeline, or another external service
    gpu: requires the real local inference runtime and CUDA
