# Purpose

Extract typed entities and relations with source provenance.

# Template

Given versioned canonical chunks, return typed entities and concept relations.
Use stable per-response entity keys and attach exact supporting canonical chunk
IDs to every entity and relation. Relations may be inferred but must never lose
their source provenance.
